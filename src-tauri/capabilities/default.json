{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for the SynapseHub dashboard.",
  "windows": ["dashboard"],
  "permissions": [
    "core:default",
    "notification:default",
    "process:default",
    "updater:default"
  ]
}
